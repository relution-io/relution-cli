{
  "name": "relution-cli",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "precommit": "npm run tslint",
    "tslint": "tslint src/**/*.ts",
    "test": "jasmine-node --verbose --autotest --watch --color --test-dir lib",
    "build": "tsc -p .",
    "api": "typedoc --out public/docs src --module commonjs --target ES5 --name Relution-Cli --exclude **/*.spec.ts",
    "watch": "tsconfig && tsc -p . -w",
    "postinstall": "typings install"
  },
  "bin": {
    "relution": "./lib/relution.js"
  },
  "author": "Pascal Brewing",
  "license": "Apache License",
  "devDependencies": {
    "jasmine-node": "^1.14.5",
    "tslint": "^3.8.1",
    "typedoc": "^0.3.12",
    "typescript": "^1.8.10"
  },
  "dependencies": {
    "inquirer": "^1.0.2"
  }
}
